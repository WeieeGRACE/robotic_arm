### 1. 🦾 机械臂与爪子 (PWM 舵机组)

*集中在左侧排针顶部，全组使用纯净动力 +Power5V (来自 XL4016) 供电。*

| **核心板引脚** | **功能定义**          | **外接说明 (接 3x6 舵机排针)** |
| -------------- | --------------------- | ------------------------------ |
| **IO15**       | TBS2701 舵机 1 (基座) | 接 信号线 (S)                  |
| **IO14**       | TBS2701 舵机 2        | 接 信号线 (S)                  |
| **IO13**       | TBS2701 舵机 3        | 接 信号线 (S)                  |
| **IO12**       | TBS2701 舵机 4        | 接 信号线 (S)                  |
| **IO11**       | TBS2701 舵机 5        | 接 信号线 (S)                  |
| **IO10**       | ZP15S 机械爪舵机      | 接 信号线 (S)                  |

### 2. 🚗 小车底盘 (RZ7899 电机驱动组)

*集中在右侧排针顶部。电机动力直接来自 +12V_BATT，此处的 IO 仅提供逻辑控制信号。*

| **核心板引脚** | **功能定义**         | **外接说明 (接 RZ7899-MS)** |
| -------------- | -------------------- | --------------------------- |
| **IO16**       | M1 右电机 IN1 (BI)   | 接 U1 的 `Pin 1 (BI)`       |
| **IO17**       | M1 右电机 IN2 (FI)   | 接 U1 的 `Pin 2 (FI)`       |
| **IO18**       | M2 左电机 IN1 (BI)   | 接 U2 的 `Pin 1 (BI)`       |
| **IO19**       | M2 左电机 IN2 (FI)   | 接 U2 的 `Pin 2 (FI)`       |

> ⚠️ **已知差异**: M2 左电机物理方向与 M1 相反（相同 PWM 信号下两轮转向相反）。
> 已在 `motor.c` 的 `motor_set_speed()` 中对 M2 取反修正。若更换电机或重新接线后方向变反，
> 删掉 motor.c 第 143 行的 `if (motor == MOTOR_M2) { target = -target; }` 即可。

### 3. 📡 通信与遥控 (外设扩展组)

*蓝牙接 +Power5V (来自 LM2596)，PS2 接收器严格接 3.3V。*

| **核心板引脚** | **功能定义**      | **外接说明**               |
| -------------- | ----------------- | -------------------------- |
| **IO20**       | 蓝牙 UART_TX      | 接 蓝牙模块 `RXD`          |
| **IO21**       | 蓝牙 UART_RX      | 接 蓝牙模块 `TXD`          |
| **IO22**       | 航模 SBUS (可选)  | 接 接收机 `Signal / SBUS`  |
| **IO26**       | PS2_CS (片选)     | 接 PS2 接收器 `CS / SEL`   |
| **IO27**       | PS2_CLK (时钟)    | 接 PS2 接收器 `CLK`        |
| **IO28**       | PS2_MISO (数据入) | 接 PS2 接收器 `DAT / MISO` |
| **IO29**       | PS2_MOSI (指令出) | 接 PS2 接收器 `CMD / MOSI` |

### 4. 🧭 传感器总线预留

*供 MPU6050 陀螺仪或 OLED 屏幕使用，使用 3.3V 供电。*

| **核心板引脚** | **功能定义**   | **外接说明**    |
| -------------- | -------------- | --------------- |
| **IO30**       | I2C_SDA (数据) | 接 传感器 `SDA` |
| **IO31**       | I2C_SCL (时钟) | 接 传感器 `SCL` |

### 5. 👁️ 核心板已被硬件绑定的引脚 (扩展板不可用)

*以下引脚在核心板内部已连接物理外设，底板排针上虽然有引出，但在分配功能时\**绝对避开**。*

- **视觉与显示：** `IO0`, `IO1`, `IO7`, `IO8` (MIPI CSI/DSI 及 I2C 共用)
- **TF卡存储：** `IO39`, `IO40`, `IO41`, `IO42`, `IO43`, `IO44` (SDIO)
- **基础交互与调试：** `IO24`, `IO25` (辅助 USB), `IO35` (板载按键), `IO51` (板载 LED)

### 6. 🟩 剩余完全闲置 IO (共计 22 个)

如果你们以后还需要加灰度巡线、超声波避障、单线激光雷达或蜂鸣器，请直接从以下引脚中任取：

- **低位：** `IO2`, `IO3`, `IO4`, `IO5`, `IO6`, `IO9`, `IO23`
- **中位：** `IO32`, `IO33`, `IO34`, `IO36`, `IO37`, `IO38`
- **高位：** `IO45`, `IO46`, `IO47`, `IO48`, `IO49`, `IO50`, `IO52`, `IO53`, `IO54`